services:
  app:
    build: .
    image: ded-char-sheet
    ports:
      - "8000:8000"
    env_file:
      - .env
    # depends_on:
    #   - mongo

  # mongo:
  #   image: mongo:latest
  #   ports:
  #     - "27017:27017"
  #   environment:
  #     - MONGO_INITDB_DATABASE=dedchar
  #   volumes:
  #     - mongo-data:/data/db

# volumes:
#   mongo-data: